apiVersion: v1
kind: Service

metadata:
  name: ai-devops-platform-service

spec:
  selector:
    app: ai-devops-platform

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000

  type: NodePort